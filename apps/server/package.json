{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "build": "run-s clean build:*",
    "build:ts": "tsc -p .",
    "dev": "run-s build wait start:watch",
    "wait": "wait-on dist/index.js",
    "start:watch": "pnpm start -- --watch",
    "start": "node dist/index.js",
    "lint": "gts lint",
    "format": "gts fix"
  },
  "devDependencies": {
    "@types/node": "^18.13.0",
    "eslint": "^8.34.0",
    "gts": "^3.1.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.8.2",
    "turbo": "latest",
    "wait-on": "^7.0.1"
  },
  "dependencies": {
    "@trpc/server": "^10.11.1",
    "zod": "^3.20.6"
  }
}
