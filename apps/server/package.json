{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc -p .",
    "dev": "wait-on dist/index.js && node --watch dist/index.js",
    "lint": "gts lint",
    "format": "gts fix"
  },
  "devDependencies": {
    "gts": "^3.1.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.8.2",
    "turbo": "latest",
    "eslint": "^8.34.0",
    "wait-on": "^7.0.1"
  },
  "dependencies": {
    "@trpc/server": "^10.11.1",
    "zod": "^3.20.6"
  }
}
